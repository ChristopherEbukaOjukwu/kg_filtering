# What H1 actually asks
For each filter individually: do surviving targets have higher publication counts than dropped targets, and by how much?
Note "targets," not "pairs": H1 is about the gene population that survives, not the edge set. A target survives a filter 
if at least one of its associations passes that filter.

We run this from: h1_mann_u.py
